import { binomial, exponential, normal, pareto } from './probability';
export declare const Probability: {
    binomial: typeof binomial;
    exponential: typeof exponential;
    normal: typeof normal;
    pareto: typeof pareto;
};
