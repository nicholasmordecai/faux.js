import { expect } from 'chai';

import { compile } from '../../src/compiler/compiler';
import { testProject } from '../utils';

describe('Compiler Tests', () => {
    it('Should run through a complete compilation', () => {
        const result = compile('', true);
    });
});
